{
  "name": "extensible_markdown_docs",
  "version": "0.1.0",
  "description": "Documentation site for Extensible Markdown (.emdd)",
  "main": "index.js",
  "scripts": {
    "build": "npx emdd site --config=./site_config.json",
    "deploy": "git subtree push --prefix build origin gh-pages",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
